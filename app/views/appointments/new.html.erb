<div class="flex-box">
  <%= link_to pet_appointments_path(@pet) do %>
    <%= render 'shared/buttons/backbtn' %>
  <% end %>
</div>

<div class="container">
  <div class="appointment-new-header">
    <h2> Create a new Appointment for <%= @pet.name %></h2>
  </div>
  <div class="appointment-form">
    <%= render 'form', appointment: @appointment %>
  </div>
</div>
