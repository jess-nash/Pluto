<%= simple_form_for [@pet, @journal_entry] do |f| %>
  <%= f.input :name, placeholder: 'title' %>
  <%= f.input :content, as: :text, placeholder: 'Write something...' %>
  <%= f.input :photos, as: :file, input_html: { multiple: true } %>
  <%= f.submit 'Create' %>
<% end %>
