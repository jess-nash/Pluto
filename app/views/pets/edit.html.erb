<div class="nav-buttons">
  <%= link_to profile_path(@pet) do %>
    <%= render 'shared/buttons/backbtn' %>
  <% end %>
</div>

<div class="container">
  <div class="pet-new-header">
    <h2 class="new-pet-name"> Update <%= @pet.name %>'s info</h2>
  </div>
  <div class="pet-entry-form">
    <%= render 'form' %>
  </div>
</div>
